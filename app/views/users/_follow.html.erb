<%= form_for(current_user.follows.build(followable_id: @user.id),
             remote: true) do |f| %>
  <%= f.hidden_field :followable_id %>
  <%= f.submit "Follow", class: "btn btn-large btn-primary" %>
<% end %>
